route:
  receiver: local-evidence-sink
  group_by: [alertname, environment]
  group_wait: 1s
  group_interval: 5s
  repeat_interval: 1h

receivers:
  - name: local-evidence-sink
    webhook_configs:
      - url: http://alert-receiver:8080/alert
        send_resolved: true
